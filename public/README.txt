Place the VitalServe logo image file here as `logo.png`.

- File path: /logo.png (served by Vite as `/logo.png`)
- Recommended size: 512x512 or similar square PNG with transparent background.

Drop your provided logo image into this folder and name it `logo.png` so the app displays it in the header.
